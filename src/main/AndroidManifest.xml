<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.diiage.clementh.poc.hugon.swapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocHugonSwapi">
        <activity android:name=".activities.PlanetsDetailActivity"></activity>
        <activity android:name=".activities.PeopleDetailActivity" />
        <activity android:name=".activities.PeopleActivity" />
        <activity android:name=".activities.StarshipsActivity" />
        <activity android:name=".activities.PlanetsActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>